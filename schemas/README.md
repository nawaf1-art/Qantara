# Schemas

This directory is for shared protocol and event definitions.

Expected scope:

- browser-to-gateway message shapes
- gateway-to-browser control events
- event timeline schema
- session and playback state payloads

The goal is to keep transport and session semantics explicit and versionable.
